<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE hibernate-configuration PUBLIC
        "-//Hibernate/Hibernate Configuration DTD 3.0//EN"
        "http://www.hibernate.org/dtd/hibernate-configuration-3.0.dtd">
<hibernate-configuration>
    <session-factory>

        <!-- ── JDBC connection ────────────────────────────────────────── -->
        <property name="hibernate.connection.driver_class">com.mysql.cj.jdbc.Driver</property>
        <property name="hibernate.connection.url">
            jdbc:mysql://localhost:3306/algo_trading?createDatabaseIfNotExist=true&amp;useSSL=false&amp;serverTimezone=Asia/Kolkata&amp;allowPublicKeyRetrieval=true
        </property>
        <property name="hibernate.connection.username">root</property>
        <property name="hibernate.connection.password"></property>

        <!-- ── Dialect ────────────────────────────────────────────────── -->
        <property name="hibernate.dialect">org.hibernate.dialect.MySQLDialect</property>

        <!-- ── DDL: creates tables if missing, alters columns if changed ─ -->
        <property name="hibernate.hbm2ddl.auto">update</property>

        <!-- ── SQL logging (set true for debugging) ───────────────────── -->
        <property name="hibernate.show_sql">false</property>
        <property name="hibernate.format_sql">false</property>

        <!-- ── Connection pool ────────────────────────────────────────── -->
        <property name="hibernate.connection.pool_size">5</property>

        <!-- ── Mapped entity classes ──────────────────────────────────── -->
        <mapping class="self.sai.stock.AlgoTrading.entity.Instrument"/>
        <mapping class="self.sai.stock.AlgoTrading.entity.Candle"/>
        <mapping class="self.sai.stock.AlgoTrading.entity.Trade"/>
        <mapping class="self.sai.stock.AlgoTrading.entity.Setting"/>
        <mapping class="self.sai.stock.AlgoTrading.entity.User"/>

    </session-factory>
</hibernate-configuration>
